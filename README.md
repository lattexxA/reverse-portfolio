# 🔍 Reverse Engineering Portfolio
Потом напишу о себе:)
# 1. Написание калькулятора на C++ и исследование его работы в дизассемблере

### 📋 Детали проекта:
**Исходный код:** `/cpp/kalkulator/kalkulator.cpp`
**Скомпилированный проект:** `/cpp/output/kalkulator/kalkulator.exe`

### 🛠️ Инструменты:
- **C++** (GCC 15.2.0)
- **IDA Free** (дизассемблер)

### 📝 Описание:
Калькулятор написан на C++ с использованием библиотеки iostream.  
Реализованы четыре основные операции через цепочку условий if-else:
- Сложение
- Вычитание  
- Деление
- Умножение

## 🔬 Анализ в IDA

### Принцип работы:

#### 1. **Первичная проверка ввода**
При анализе кода в IDA обнаружил, что первой проверкой **проверяется, является ли числом** переменная, отвечающая за выбор действия.  
Если ввод не является числом → программа сразу выводит ошибку "Неверное число".

#### 2. **Цепочка условий if-else**
Если ввод корректен (число), происходит последовательная проверка:
- **Проверяется, является ли число единицей** → если да, выполняется сложение
- Если нет → проверка равенства 2 → вычитание
- Аналогично для 3 (деление) и 4 (умножение)
- Если не равно ни одному из значений 1-4 → вывод ошибки

#### 3. **Структура потока выполнения**
Важное наблюдение: все 5 веток выполнения (4 операции + ошибка) в конце ведут на `system("pause")`.  
Это означает, что даже при успешном выполнении операции (например, сложении) программа переходит к паузе.

### Ключевые находки:
1. **При вводе нечислового значения** → немедленный вывод ошибки (не запрашивает дополнительные числа)
2. **При вводе числа вне диапазона 1-4** → программа всё равно запрашивает два числа для операций, хотя они не будут использованы

# 2 Иследования while цикла 

### 📋 Детали проекта:
**Исходный код:** `/cpp/while/while.cpp`

**Скомпилированный проект:** `/cpp/output/while/while.exe`

### 🛠️ Инструменты:
- **C++** (GCC 15.2.0)
- **IDA Free** (дизассемблер)

### 📝 Описание:
простой цикл while с отсчетом от 0 до 5 

### Принцип работы: 
с начала у нас есть функция if else где проверяетсяь наще условие если оно верно то нас ведут на выход если нет то выполняються условия цикла и происходит скачек в начало цикла с помощью jnz где цикл будет повторяться до того момента пока наши условия не совпадут 